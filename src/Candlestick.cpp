 #include "Candlestick.h"

Candlestick::Candlestick(const std::string& date_, double open_, double high_, double low_, double close_)
    : date(date_), open(open_), high(high_), low(low_), close(close_) {}
